from fastapi import FastAPI
from fastapi.responses import JSONResponse

app = FastAPI()

@app.get("/")
def home():
    return {"message": "GaripMobilAI başarıyla çalışıyor 🚀"}

@app.get("/hello/{name}")
def say_hello(name: str):
    return JSONResponse(content={"message": f"Merhaba {name}, sistem aktif!"})

# Railway otomatik olarak PORT değişkenini belirler
# Ancak yerelde test ederken port 8000'de çalışır
if __name__ == "__main__":
    import uvicorn
    import os
    port = int(os.environ.get("PORT", 8000))
    uvicorn.run("main:app", host="0.0.0.0", port=port)
