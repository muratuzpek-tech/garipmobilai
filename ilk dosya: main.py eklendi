from fastapi import FastAPI

# FastAPI uygulaması oluştur
app = FastAPI()

# Ana URL
@app.get("/")
def read_root():
    return {"message": "🚀 GaripMobil AI çalışıyor!"}

# Test endpoint
@app.get("/ping")
def ping():
    return {"status": "ok", "message": "pong!"}

# Örnek API (dinamik parametre ile)
@app.get("/hello/{name}")
def say_hello(name: str):
    return {"message": f"Merhaba {name}! GaripMobil AI seni selamlıyor 🤖"}
